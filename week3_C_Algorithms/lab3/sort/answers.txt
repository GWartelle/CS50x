sort1 uses: bubble sort

How do you know?: it runs faster on significantly faster on sorted lists

sort2 uses: merge sort

How do you know?: it runs faster than the other sorts most of the time, and performs relatively the same with random and sorted lists

sort3 uses: selection sort

How do you know?: it runs slower than the other sorts most of the time, and performs relatively the same with random and sorted lists
